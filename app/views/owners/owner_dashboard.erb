<nav class="navbar navbar-inverse navbar-fixed-top" style="margin-bottom: 70px;">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/owners/<%=current_owner.id%>">Taco Bomb! <%=current_owner.name%></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li>
          <a href="/owners/<%=current_owner.id%>/create_new_menu_item">Create Menu Item</a>
        </li>
        <li>
          <a href="/owners/<%=current_owner.id%>/menu_items">Your Menu Items</a>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <a href="/logout">Log Out</a>
        </li>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>
<div>
  <div style="margin-top: 70px;">
    <ul>
      <% @orders.each do |order| %>
        <li>
          <h3><%=order.created_at%></h3>
            <ul>
              <%order.menu_items.each do |menu_item|%>
                <li><%=menu_item.name%></li>
              <%end%>
            </ul>
            <%=order.customer.name%>  /  <%=order.address%>
            <form action="/owners/<%=order.id%>/close" method="post">
              <input type="hidden" value="PATCH" id="hidden" name="_method">
              <input type="submit" name="close" value="close">
            </form>
        </li>
      <%end%>
    </ul>
  </div>
</div>
